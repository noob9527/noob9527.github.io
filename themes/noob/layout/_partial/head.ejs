<meta charset="utf-8">
<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1"/>

<% if(page.noindex){ %>
    <meta name="robots" content="noindex"/>
<% } %>

<%
    var title = page.title;

    if (is_archive()){
        title = 'Archives';

        if (is_month()){
            title += ': ' + page.year + '/' + page.month;
        } else if (is_year()){
            title += ': ' + page.year;
        }
    } else if (is_category()){
        title = 'Category: ' + page.category;
    } else if (is_tag()){
        title = 'Tag: ' + page.tag;
    }
%>

<title><%- (title ? title : theme.title) + ' - ' + theme.description %></title>

<% if(page.keywords){ %>
    <meta name="keywords" content="<%- page.keywords %>">
<% } else if(page.tags && page.tags.length){ %>
    <meta name="keywords" content="<%- page.tags.map(function(t){return t.name;}).join() %>">
<% } else if(theme.keywords){ %>
    <meta name="keywords" content="<%- theme.keywords %>">
<% } %>

<%-
   open_graph({
    twitter_id: theme.twitter,
    google_plus: theme.google_plus,
    fb_admins: theme.fb_admins,
    fb_app_id: theme.fb_app_id
  })
%>

<% if(theme.favicon ){ %>
    <link rel="shortcut icon" type="image/x-icon" href="<%- url_for(theme.favicon) %> " />
    <link rel="Bookmark" type="image/x-icon" href="<%- url_for(theme.favicon) %> " />
    <link rel="icon" type="image/x-icon" href="<%- url_for(theme.favicon) %> " />
<% } %>

<% if(theme.google_site_verification){ %>
<meta name="google-site-verification" content="<%- theme.google_site_verification %>" />
<% } %>
<% if(theme.baidu_site_verification){ %>
<meta name="baidu-site-verification" content="<%- theme.baidu_site_verification %>" />
<% } %>

<% if (theme.rss){ %>
<link rel="alternative" href="<%- theme.rss %>" title="<%= config.title %>" type="application/atom+xml">
<% } %>

<%- js(['vendors/jquery/jquery', 'vendors/bootstrap/js/bootstrap', 'js/script']) %>
<%- css('vendors/font-awesome/css/font-awesome.min') %>
<%- css('css/style') %>

<script type="text/javascript" id="hexo.configuration">
  var CONFIG = {
    duoshuo: {
      userId: '<%- theme.duoshuo.user_id || '' %>',
      author: '<%- theme.duoshuo.admin_nickname || __('author')%>'
    },
    search: {
      //path: '<%- url_for(theme.search.path) %>'  
      path: '<%- config.root + theme.search.path %>'
    }  
  };
</script>